const scenes=[...document.querySelectorAll('.scene')];let idx=0,step=0;const phase=document.getElementById('phase'),count=document.getElementById('count');
function fragments(){return [...scenes[idx].querySelectorAll('.fragment')]}
function render(){scenes.forEach((s,i)=>s.classList.toggle('active',i===idx));const fs=fragments();fs.forEach((f,i)=>f.classList.toggle('visible',i<step));phase.textContent=scenes[idx].dataset.phase||'';count.textContent=String(idx+1).padStart(2,'0')+' / '+String(scenes.length).padStart(2,'0');const p=idx/(scenes.length-1);document.getElementById('m1').style.setProperty('--v',Math.min(100,20+p*80)+'%');document.getElementById('m2').style.setProperty('--v',Math.min(100,p*105)+'%');document.getElementById('m3').style.setProperty('--v',Math.max(0,(p-.35)*145)+'%')}
function next(){const fs=fragments();if(step<fs.length){step++;render();return}if(idx<scenes.length-1){idx++;step=0;render()}}
function prev(){if(step>0){step--;render();return}if(idx>0){idx--;step=fragments().length;render()}}
document.addEventListener('keydown',e=>{if(['ArrowRight',' ','PageDown'].includes(e.key)){e.preventDefault();next()}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();prev()}if(e.key.toLowerCase()==='f')document.documentElement.requestFullscreen?.();if(e.key.toLowerCase()==='n')document.getElementById('notes').classList.toggle('show')});document.getElementById('stage').addEventListener('click',e=>{if(e.target.tagName!=='BUTTON')next()});
let timer=null,secs=300;const t=document.getElementById('timerText'),btn=document.getElementById('timerBtn');btn.addEventListener('click',e=>{e.stopPropagation();if(timer){clearInterval(timer);timer=null;btn.textContent='Resume timer';return}btn.textContent='Pause timer';timer=setInterval(()=>{secs--;t.textContent=String(Math.floor(secs/60)).padStart(2,'0')+':'+String(secs%60).padStart(2,'0');if(secs<=0){clearInterval(timer);timer=null;btn.textContent='Complete'}},1000)});
const prompts=['Have you described the outcome—or only the implementation?','Is the ask concrete?','Does this audience share your context?','Where should this signal appear again?'];let pi=0;setInterval(()=>{const el=document.getElementById('promptBar');if(el){pi=(pi+1)%prompts.length;el.textContent=prompts[pi]}},12000);render();
